import math

nota1 = float(input("insira a nota 1 (Peso 2): "))
nota2 = float(input("insira a nota 2 (Peso 3): "))
nota3 = float(input("insira a nota 3 (Peso 5): "))

calcMedia = (nota1 * 0.2) + (nota2 * 0.3) + (nota3 * 0.5) 

print(f"Media = {calcMedia}")